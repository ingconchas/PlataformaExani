import { InicioClient } from "./inicio-client";

/** Inicio — «Mi progreso» de la alumna (LUI-24 · Diseño 23): puntaje reciente vs meta,
 *  próximo simulacro y último resultado. */
export default function Page() {
  return <InicioClient />;
}
